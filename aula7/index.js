// Constantes não podem iniciar vazias.
// Constantes não podem ser modificadas.

const primeiroNumero = 5;
const segundoNumero = 10;
const resultado = primeiroNumero * segundoNumero;
const resultadoDuplicado = resultado * 2
let resultadoTriplicado = resultado * 3;

console.log(primeiroNumero, '*', segundoNumero, '=' ,resultado);
console.log(resultado, '* 2 =', resultadoDuplicado);
console.log(resultado, '* 3 =', resultadoTriplicado)